# =============================================================================
# PRODUCTION REQUIREMENTS (Minimal) - Clinical Trial Dropout API
# =============================================================================
# Optimized for AWS Free Tier (t2.micro, 8GB storage)
# Target image size: < 1GB
# =============================================================================

# Core Data (minimal versions)
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# ML Runtime (only what's needed for inference)
scikit-learn>=1.5.0,<1.6.0
# Note: If using XGBoost/LightGBM model, uncomment as needed:
# xgboost>=2.0.0,<3.0.0
# lightgbm>=4.0.0,<5.0.0

# Model Serialization
joblib>=1.3.0,<2.0.0

# API Framework
fastapi>=0.109.0,<1.0.0
uvicorn>=0.27.0,<1.0.0
pydantic>=2.5.0,<3.0.0
